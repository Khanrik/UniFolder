(library
  (name Syntax)
  (modules Syntax)
  (libraries Partial Utility)
  (modes byte native))

(library
  (name Utility)
  (modules Utility)
  (modes byte native))

(library
  (name Partial)
  (modules Partial)
  (modes byte native))
