## (1) lease read the document Steam Boiler Case, available on Brightspace.
### (a) List the components of the steam boiler system.


### (b) Name the different modes that the controller can be in.


### (c) What will happen to the water quantity if one pump fails and steam output is at maximum?


## (2) In the lectures we used VDM VSCode to verify, check and analyse our Steam Boiler model in a number of different ways. In the lecture slides and VDM VSCode tutorial the terms *static analysis* and *simulation* were used.
### (a) What is static analysis? Give an example from VDM VSCode.


### (b) What is simulation? Give an example from VDM VSCode.


### (c) What does the term testing refer to? Give an example from VDM VSCode.


## (3) In the VDM VSCode tutorial we worked on SwapExample project.
### (a) Install VDM VSCode on your computer, following the slides in the VDM VSCode tutorial we went through in the lecture.


### (b) Create your own SwapExample project (following the steps from the tutorial). Change the VDM-RT model so that the final sequence in second list is the reverse of the initial sequence in first list, after calling DoSwap(). The challenge is that inside the Swap-Next function you can only use the operations: hd, tl, ˆ and variable assignment. For example, after executing DoSwap() the output printed to the console should be: newSeqSwap().DoSwap() = [5, 4, 3, 2, 1]


### (c) In the tutorial we only used the “Step Into” button in debug mode. What does the “Step Over” button do in debug mode? Explain with an example from the SwapExample project.


### (d) What does the “Step Out” button do in debug mode? Explain with an example from the SwapExample project.


### (e) In the tutorial we placed the breakpoint at the first line of the DoSwap() operation. Where can you put the breakpoint so that clicking the “Resume” button causes execution to pause repeatedly?


### (f) What is the difference between clicking the “Run RunDoSwap” and “Debug Run-DoSwap” buttons? (using the run configuration name from the VDM VSCode tutorial)


### (g) In the “Breakpoints” panel we can check and uncheck breakpoints. What is the effect of checking and unchecking a breakpoint?